/// R121 filesystem counterpart — the real `ApplicationConfigBuilder` is
/// declared in `api/types/application_config_builder.rs` and re-exported
/// flatly as `crate::api::ApplicationConfigBuilder`.
pub type ApplicationConfigBuilder = ();
